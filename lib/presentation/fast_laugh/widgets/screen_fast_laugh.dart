import 'package:flutter/material.dart';

class ScreenFastlaughPage extends StatelessWidget {
  const ScreenFastlaughPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Fast laugh page'),
      ),
    );
  }
}
